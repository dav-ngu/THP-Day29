<div class="div_text_centered">
<h2 class="h1-responsive font-weight-bold my-5">Bienvenue sur la page de création de potin</h2>
<div class=" border border-light p-5">
    <p> Pour cela, tu trouveras ci-dessous le formulaire à compléter :</p>
    
    <%= form_tag url_for(action: 'create'), method: "post" do %>
      <div class="card text-center w-50">
        <%= label_tag 'Titre' %>
        <%= text_field_tag 'title' %>
        <br />
  
        <%= label_tag 'Contenu' %>
        <%= text_area_tag 'content' %>
        <br />
  
        <%= submit_tag "Générer le potin" %>
  
      <% end %>
      </br>
      <% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
</div>
</div>
</div>
<a href="<%= gossips_path %>" class="btn btn-secondary m-3 rounded-pill">Revenir au menu</a>
