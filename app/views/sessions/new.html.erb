<div class="div_text_centered">
<h2 class="h1-responsive font-weight-bold my-5">Page de connexion</h2>
        <div class=" border border-light p-5">
        <p class="h4 mb-4">Pour te connecter, complète les informations ci-dessous :</p>

        <%= form_tag url_for(action: 'create'), method: "post" do %>
    
        <p><%= label_tag 'E-mail :' %></p>
        <%= text_field_tag 'email', nil,  class: "form-control mb-2"%>

        <p><%= label_tag 'Mot de passe :' %></p>
        <%= text_field_tag 'password', nil,  class: "form-control mb-2"%>    
           
        </br>
        
        <%= submit_tag "Valider la connexion" %>
        <a href="<%= new_user_path %>" class="btn btn-primary m-3 rounded-pill">Par encore inscrit ? Clique ici !</a>
        </div>

      <% end %>
      </div>